_site/
.jekyll-cache/
.jekyll-metadata

# Gems are vendored locally; Gemfile.lock is tracked so CI resolves identically
/vendor/
.bundle/

# CV LaTeX source and its build output: kept locally, not tracked
# The published copy lives at files/cv.pdf
cv.tex
/cv.pdf
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz

.DS_Store
